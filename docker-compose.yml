services:
  siteproxy:
    image: node:22
    container_name: siteproxy-node
    working_dir: /app
    volumes:
      - ./:/app
    command: bash -c "npm install && node index.js"
    ports:
      - "5006:5006"
    restart: always
